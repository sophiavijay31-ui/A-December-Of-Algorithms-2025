#include<stdio.h>
#include<math.h>
int main()
{
    int num,a=0,x,y;
    printf("Enter a number:");
    scanf("%d",&num);
    for(x=1;x<=num;x++)
    {
    for(y=1;y<=i;y++)
    {
        if(sqrt(x)==y)
        {
        printf(" %d",x);
        a++;
        }
    }
    break;
    }
printf("\n%d",a);
}

